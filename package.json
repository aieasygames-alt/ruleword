{
  "name": "ruleword",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "cd astro-app && pnpm dev",
    "build": "cd astro-app && pnpm install && npx tsx scripts/generate-images.ts && pnpm build && cp -r dist ../dist",
    "build:ci": "cd astro-app && npx tsx scripts/generate-images.ts && pnpm build && cp -r dist ../dist",
    "preview": "cd astro-app && pnpm preview"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "satori": "^0.26.0"
  }
}
