{
  "name": "ruleword",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "cd astro-app && pnpm dev",
    "build": "cd astro-app && pnpm build",
    "preview": "cd astro-app && pnpm preview",
    "deploy": "pnpm build && gh-pages -d astro-app/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
