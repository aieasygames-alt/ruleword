{
  "name": "ruleword",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "cd astro-app && pnpm dev",
    "build": "rm -rf dist && cd astro-app && pnpm install && pnpm build && cp -r dist ../dist",
    "preview": "cd astro-app && pnpm preview",
    "deploy": "pnpm build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
